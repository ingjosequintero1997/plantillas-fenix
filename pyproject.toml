[project]
name = "fenix-data"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "pandas",
    "python-multipart",
    "rapidfuzz",
    "python-dateutil",
    "numpy",
    "openpyxl",
    "pydantic",
    "sqlalchemy",
    "psycopg2-binary",
]

[tool.uv]
index-url = "https://pypi.org/simple"
